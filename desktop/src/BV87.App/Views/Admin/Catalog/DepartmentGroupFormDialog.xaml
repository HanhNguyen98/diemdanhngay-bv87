<shell:AppDialogWindow x:Class="BV87.App.Views.Admin.Catalog.DepartmentGroupFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:BV87.App.Shell"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource SurfacePageBrush}">
    <Border Margin="16" Padding="20" Background="{StaticResource SurfaceWhiteBrush}"
            BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8" MinWidth="380">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                       Foreground="{StaticResource NavyBrush}" Margin="0,0,0,16"/>

            <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerFgBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>

            <TextBlock Text="Mã nhóm" Style="{StaticResource FormLabelStyle}"/>
            <TextBox x:Name="CodeBox" IsReadOnly="True" Style="{StaticResource FilterTextBoxStyle}"
                     Background="{StaticResource PrimaryLightBrush}" Margin="0,0,0,12"/>

            <TextBlock Text="Tên nhóm" Style="{StaticResource FormLabelStyle}"/>
            <TextBox x:Name="GroupNameBox" Style="{StaticResource FilterTextBoxStyle}" Margin="0,0,0,12"/>

            <TextBlock Text="Thứ tự sắp xếp" Style="{StaticResource FormLabelStyle}"/>
            <TextBox x:Name="SortOrderBox" Style="{StaticResource FilterTextBoxStyle}" Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy" Padding="16,8" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button x:Name="SaveButton" Content="Lưu" Style="{StaticResource PrimaryButtonStyle}"
                        Padding="16,8" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</shell:AppDialogWindow>
