<shell:AppDialogWindow x:Class="BV87.App.Views.Admin.Utilities.UnlockRejectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:BV87.App.Shell"
        xmlns:controls="clr-namespace:BV87.App.Controls"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource SurfacePageBrush}">
    <Border Margin="20" Padding="20" Background="{StaticResource SurfaceWhiteBrush}"
            BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <controls:DialogContextHeader x:Name="ContextHeader" Grid.Row="0"/>
            <TextBlock x:Name="HintText" Grid.Row="1" TextWrapping="Wrap"
                       Foreground="{StaticResource ContentMutedBrush}" Margin="0,0,0,12"/>
            <TextBox x:Name="NoteBox" Grid.Row="2" MinHeight="72" AcceptsReturn="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
                <Button x:Name="RejectButton" Content="Từ chối" Width="90"
                        Style="{StaticResource PrimaryButtonStyle}" Click="RejectButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</shell:AppDialogWindow>
