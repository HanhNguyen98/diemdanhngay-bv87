<UserControl x:Class="BV87.App.Controls.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed"
             IsHitTestVisible="False">
    <Border x:Name="ToastBorder"
            MaxWidth="420"
            MinWidth="280"
            Padding="14,12"
            CornerRadius="12"
            BorderThickness="1"
            IsHitTestVisible="True">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="12" Opacity="0.12" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="IconText"
                       FontFamily="{StaticResource IconFont}"
                       FontSize="{StaticResource FontSizeLg}"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       TextWrapping="Wrap"
                       FontSize="{StaticResource FontSizeMd}"
                       Foreground="{StaticResource ContentHeadingBrush}"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="2"
                    Style="{StaticResource GhostButtonStyle}"
                    Padding="6,6"
                    MinWidth="32"
                    MinHeight="32"
                    VerticalAlignment="Top"
                    Click="CloseButton_Click"
                    ToolTip="Đóng">
                <TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE711;" FontSize="{StaticResource FontSizeSm}"/>
            </Button>
        </Grid>
    </Border>
</UserControl>
