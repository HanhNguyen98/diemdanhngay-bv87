<Window x:Class="BV87.App.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BV87 — Đăng nhập"
        Height="420" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource SurfacePageBrush}">
    <Border Margin="24" Padding="24" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="8">
        <StackPanel>
            <TextBlock Text="BV87 Chấm công" FontSize="22" FontWeight="Bold" Foreground="{StaticResource NavyBrush}" Margin="0,0,0,4"/>
            <TextBlock Text="Bệnh viện Quân y 87" Foreground="{StaticResource ContentMutedBrush}" Margin="0,0,0,20"/>

            <TextBlock Text="Tên đăng nhập" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Height="32" Padding="8,4" Margin="0,0,0,12"/>

            <TextBlock Text="Mật khẩu" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Height="32" Padding="8,4" Margin="0,0,0,8"/>

            <TextBlock x:Name="ErrorText" Foreground="#EF4444" TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>

            <Button x:Name="LoginButton" Content="Đăng nhập" Height="36" Margin="0,0,0,12"
                    Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="LoginButton_Click"/>

            <Button Content="Chế độ kiosk (không đăng nhập)" Height="32"
                    Background="Transparent" Foreground="{StaticResource PrimaryBrush}" BorderThickness="0" Cursor="Hand"
                    Click="KioskButton_Click"/>
        </StackPanel>
    </Border>
</Window>
