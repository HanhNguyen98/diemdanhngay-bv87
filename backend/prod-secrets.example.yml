# Copy to prod-secrets.yml on server (gitignored) or set DB_* env vars.
spring:
  datasource:
    username: "diemdanh_user"
    password: "your-production-password-here"

# Optional: JWT secret override (min 32 chars). Else app.security.jwt.secret / JWT_SECRET.
# JWT_SECRET=change-me-production-min-32-chars
