# Copy to prod-secrets.yml on server (gitignored) or set DB_* env vars.
spring:
  datasource:
    username: "diemdanh_user"
    password: "your-production-password-here"

# Optional overrides (or use deploy/.env):
# APP_SECURITY_CORS_ALLOWED_ORIGIN_PATTERNS=http://192.170.182.14:8081,https://diemdanh.example.com
# SESSION_COOKIE_SECURE=false
# CF_TUNNEL_TOKEN=...  (only for cloudflared profile)
